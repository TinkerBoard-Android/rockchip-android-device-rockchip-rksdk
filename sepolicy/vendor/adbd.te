allow adbd oemfs:dir { search };

allow adbd socket_device:sock_file { write };
allow adbd rknn_server:unix_stream_socket { connectto };

rw_rockchip_graphic_device(adbd)
set_prop(adbd,ctl_start_prop)
